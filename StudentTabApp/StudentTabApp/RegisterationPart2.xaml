<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="StudentTabApp.RegisterationPart2"
             Title="Registeration Part 2">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Class: "/>
            <Entry x:Name="ClassEntry" Placeholder="Class" Text="{Binding Class}"/>
            <Label Text="Class Math: "/>
            <Entry x:Name="ClassNumEntry" Placeholder="0" Text="{Binding ClassNo}"/>
            <Label Text="Like Math?"/>
            <Switch x:Name="LikeMathSwitch" IsToggled="{Binding LikeMath}" ThumbColor="Green" OnColor="Red"/>
            <Label Text="Like Computer Science?"/>
            <Switch x:Name="LickCsSwitch" IsToggled="{Binding LikeCs}" ThumbColor="Green" OnColor="Red"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>